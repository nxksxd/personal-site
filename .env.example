# Frontend → backend API base URL.
# Dev default (when unset) is http://localhost:8000.
# In production set this to the deployed backend URL, e.g. https://api.example.com
VITE_API_URL=http://localhost:8000
